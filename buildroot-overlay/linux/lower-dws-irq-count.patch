diff --git a/drivers/spi/spi-dw-mmio.c b/drivers/spi/spi-dw-mmio.c
index 21782006f..d73732b59 100644
--- a/drivers/spi/spi-dw-mmio.c
+++ b/drivers/spi/spi-dw-mmio.c
@@ -277,7 +277,11 @@ static int dw_spi_canaan_k230_init(struct platform_device *pdev,
 	memset(dwsmmio->dws.irqs, 0, sizeof(dwsmmio->dws.irqs));
 	dwsmmio->dws.ip = DW_HSSI_ID;
 	dwsmmio->dws.caps = DW_SPI_CAP_INDEP_IRQ;
+#if 0
 	for (i = 1; i < ARRAY_SIZE(dwsmmio->dws.irqs); i++) {
+#else
+	for (i = 1; i < 9; i++) {
+#endif
 		irq = platform_get_irq(pdev, i);
 		if (irq < 0)
 			return 0;
